"""First-class entry points (apps) for wenjia-agent.

The reusable agent core lives in the ``app`` package; ``apps`` holds the
runnable adapters (web backend, and future channels) that consume it.
"""
